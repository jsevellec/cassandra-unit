widget:
  # A decimal written unquoted into a text column. YAML reads 1.10 as the double 1.1, so the exact
  # form the fixture wrote cannot be recovered - the loader has to refuse rather than store "1.1".
  - id: 1690e8da-5bf8-49e8-9583-4dff8a570b01
    label: 1.10
